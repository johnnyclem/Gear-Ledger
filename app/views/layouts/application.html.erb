<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>
    <%= stylesheet_link_tag "application", "token-input-facebook", "overcast/jquery-ui-1.8.14.custom" %>
    <%= javascript_include_tag "application", "jquery-1.5.1.min", "jquery-ui-1.8.14.custom.min", "jquery.tokeninput", "tablesort" %>
    <%= csrf_meta_tag %>
    <%= yield(:head) %>

  </head>
  <body>
    <div id="container">
		<div id="signin"><br>
		    <% if signed_in? -%>
			<%= current_user.email %>
		      <%= link_to "Sign out", sign_out_path, :method => :delete %>
		    <% else -%>
		    <% end -%></div>
	
      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
